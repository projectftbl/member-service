var Friend = require('../friend')
  , Finder = require('../finder');

module.exports = function(account) {
  // Find account friends

  // Go through each to see if friend is member

  // Friend them
};