var Follow = require('../follow')
  , Finder = require('../finder');

module.exports = function(account) {
  // Find account following

  // Go through each to see if following is member

  // Follow them
};